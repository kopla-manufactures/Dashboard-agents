# agents/

Esta carpeta contiene el estado reportado por agentes de Claude (Cowork),
uno por departamento: `agents/<slug-departamento>.json`.

No editar a mano salvo para pruebas. El protocolo completo está en
`docs/protocolo-agentes.md` en la rama `main`.

Esta rama (`data`) es solo almacenamiento de estado — nunca se despliega
ni se mergea a `main`.
